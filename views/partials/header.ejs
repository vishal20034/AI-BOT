<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %> · AI-BOT</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link
    href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap"
    rel="stylesheet"
  />
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
  <nav class="navbar">
    <div class="nav-inner">
      <a href="/" class="brand">
        <span class="brand-logo">AI</span>
        <span class="brand-text">JobBot</span>
      </a>
      <div class="nav-links">
        <a href="/" class="<%= active === 'dashboard' ? 'active' : '' %>">Dashboard</a>
        <a href="/upload" class="<%= active === 'upload' ? 'active' : '' %>">Resume</a>
        <a href="/email/history" class="<%= active === 'history' ? 'active' : '' %>">History</a>
      </div>
    </div>
  </nav>
  <main class="container">
